apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pal-tracker
spec:
  rules:
    - host: development.{DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pal-tracker-development
                port:
                  number: 8080